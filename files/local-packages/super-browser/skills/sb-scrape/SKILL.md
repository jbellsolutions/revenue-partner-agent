---
name: sb-scrape-local
description: Plan source-grounded read-only scraping through the local Super Browser runtime.
---

# Super Browser read-only scraping

This is a local planning/reference skill packaged with Revenue Partner. There is no hosted scraper bridge or live endpoint in this release.

For data gathering, use the configured local `super-browser` MCP server after an Orgo image is built and the constrained local lane passes `super-browser doctor`. Hosted providers remain planning/reference-only regardless of credentials. Follow the five-round council, preserve provenance, deduplicate records, and verify requested counts. Production writes and authenticated mutations remain non-executable.

Never downscope a bulk deliverable without reporting the blocker and achieved count.
