# Hyperbrowser provider record

Planning/reference only. Execution is blocked before provider construction because this image cannot enforce Hyperbrowser's target DNS answers, redirects, or connected peer addresses. Credentials do not change readiness. Do not create scrape jobs, submit URLs, or run provider live tests from this image.
