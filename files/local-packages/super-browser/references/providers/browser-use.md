# Browser Use provider record

Planning/reference only. The autonomous browser loop cannot be constrained to read-only behavior by prompt text, so execution is blocked before provider construction for every task. Credentials do not change readiness. Do not create sessions, navigate, or run provider live tests from this image.
